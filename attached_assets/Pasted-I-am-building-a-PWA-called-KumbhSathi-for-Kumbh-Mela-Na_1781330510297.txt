I am building a PWA called KumbhSathi for Kumbh Mela Nashik 2027.

IMPORTANT RULES:
- Always edit ROOT scripts/ folder (NOT public/scripts/)
- Always edit ROOT index.html (NOT public/index.html)
- Always edit ROOT styles/ folder (NOT public/styles/)
- Never break existing working features
- Pure HTML, CSS, JavaScript only (no frameworks)

GAS URL: https://script.google.com/macros/s/AKfycbyp_E-2tqiBfAtswJIxIeeq2iH6gwMjjlPZwlxxijqU6RdfZW8UOlcM83Gd9Yay7ZbufQ/exec
The GAS returns JSON array of objects with header names as keys e.g. {Name:"...", Type:"..."}

CURRENTLY WORKING (DO NOT BREAK):
- News page fetching from Google Sheet ✅
- Sponsors section on About page ✅
- Crowd density page (scripts/crowd2.js) ✅
- Groups feature ✅
- Lost & Found ✅
- Weather widget ✅
- Share button via WhatsApp ✅
- Language switcher (Hindi/Marathi/English) ✅
- Android back button navigation ✅
- Dark mode toggle (partially working) ✅

BUG 1 — STAY PAGE STUCK ON LOADING:
- fetchAndRenderStay() in scripts/app.js fetches ?sheet=Stay
- Google Sheet "Stay" tab has columns: Name, Type, Address, Price, Phone, Rating, Description, Image, Sponsored
- The page shows "Loading..." forever and never renders cards
- Fix fetchAndRenderStay() completely:
  * Use simple: category = (r['Type'] || 'hotel').toLowerCase().trim()
  * Map 'dharamshala' to 'dharam' only, everything else keep as is
  * sponsored = (r['Sponsored'] === true || r['Sponsored'] === 'TRUE' || r['Sponsored'] === 'true')
  * If fetch fails show fallback hardcoded STAY_DATA
  * Add proper error handling with .catch()
  * Show "No accommodations found" if rows empty instead of staying on loading

BUG 2 — SCHEDULE TIME NOT SHOWING:
- Google Sheet "Schedule" tab has columns: Date, Event, Time, Location, Description, Category, Image
- Time column value is not showing on schedule cards
- Fix time mapping to use r['Time'] as plain string
- Show time prominently on each card

BUG 3 — AD SLOTS MANAGEMENT (Home page + Map page):
- There are ad slots on the Home page and Map page
- These should be manageable via Google Sheet
- Create a new Google Sheet tab called "Ads" with columns:
  * Slot (values: home, map)
  * Title
  * Image
  * Link
  * Active (TRUE/FALSE)
  * BgColor (optional hex color)
- Add fetchAndRenderAds() function in scripts/app.js that:
  * Fetches ?sheet=Ads from GAS
  * Filters by Slot name and Active=TRUE
  * Renders a styled banner/card in the ad slot
  * If no active ad for a slot, hides the slot div completely
- Home page ad slot id: 'home-ad-slot'
- Map page ad slot id: 'map-ad-slot'
- Make sure both ad slot divs exist in index.html
- Call fetchAndRenderAds() on app init

BUG 4 — DARK MODE NOT WORKING IN MORE/ABOUT SECTION:
- Dark mode toggle works on most pages but breaks in the More/About section
- The More section cards, info-blocks, gallery items, settings list are not changing to dark colors
- Fix dark mode CSS in ROOT styles/main.css to cover ALL elements in More/About page:
  * .info-block background
  * .settings-list items
  * .more-menu-item background
  * .gallery-item background
  * .sponsors-container cards
  * Any white/light backgrounds in More section
- Also fix dark mode for:
  * Filter chips on News/Stay/Schedule pages
  * Form inputs in Lost & Found page
  * Emergency accordion items
  * Transport cards
  * Crowd density cards
- Make sure dark mode class 'dark-mode' on body covers ALL pages completely

BUG 5 — MULTILINGUAL INCOMPLETE:
- After renderStay(), renderSchedule(), renderAkharas(), renderNews() always call applyTranslations()
- Add missing translation keys in scripts/translations.js for all 3 languages:
  * our_sponsors, sponsored_tag, loading, no_results, read_more, read_less
  * home_ad_label: 'Sponsored' / 'प्रायोजित' / 'प्रायोजित'

After ALL fixes:
- Run: git add . && git commit -m "fix: stay loading, ad slots, dark mode more section, multilingual" && git push origin main
- Verify no existing features are broken
- Test dark mode on ALL pages especially More section